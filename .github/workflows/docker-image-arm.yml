# This workflow has been superseded by docker-image.yml which now builds
# multi-arch images natively using ubuntu-24.04-arm (linux/arm64) and
# ubuntu-latest (linux/amd64) runners without QEMU.
#
# This file is kept for reference only and is disabled.
name: '[DEPRECATED] Docker Image CI for SQS CONSUMER ARM'

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is deprecated. Use docker-image.yml instead.'
        required: false

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated
        run: |
          echo "This workflow is deprecated."
          echo "ARM builds are now handled natively by docker-image.yml using ubuntu-24.04-arm."
          exit 0
      
      
    

